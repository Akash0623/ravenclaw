# Ravenclaw Environment Configuration

# Gateway security (generate with: openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=CHANGE_ME

# Model providers
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME
OPENROUTER_API_KEY=sk-or-CHANGE_ME

# Channels
TELEGRAM_BOT_TOKEN=CHANGE_ME
# WhatsApp uses QR-code login via Baileys — no token needed
